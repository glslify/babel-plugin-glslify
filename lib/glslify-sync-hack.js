module.exports = function (baseDir, stringInput, optionInput) {
  // TODO: spawn glslify as a subprocess and synchronously wait until execution
  // completes
  // https://nodejs.org/api/child_process.html#child_process_child_process_execfilesync_file_args_options
  return 'GLSLIFY HACK'
}
